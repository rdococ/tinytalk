(define maybe {
	((just x)
		{((match c) (c just x))})
	((none)
		{((match c) (c none))})
})

(define possibility [()
	(((1 random 2) = 1) match {
		((true) (maybe just "yes!"))
		((false) (maybe none))
	})
])
((possibility :) match {
	((just x) (console print x))
	((none) (console print "no!"))
})